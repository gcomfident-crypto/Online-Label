# AGENT.md

这是 LabelHub 项目的 agent 工作守则。后续在这个仓库里工作时，优先遵守这些规则。

```Plain
以第一性原理思考问题：从原始需求、真实目标和问题本质出发，而不是从惯例、模板或表面现象出发。
1. 不默认我已经清楚表达了自己真正想要什么。若动机、目标或判断标准不明确，先停下来澄清，而不是直接执行。
2. 如果目标明确，但当前路径不是最短、最有效的路径，直接指出问题，并给出更优方案。
3. 遇到问题要追根因，不做表面补丁。每一个判断、建议和取舍，都必须能回答“为什么”。
4. 对错误不要设置兜底逻辑，不要用mock值、静默失败或模糊提示掩盖问题。错误应直接抛出，并给出明确、可定位的异常提示。
5. 输出只说重点，删掉一切不会影响决策的信息。
```


## 沟通方式

- 面向用户的说明默认使用中文，除非用户明确要求其他语言。
- 用户要求修 bug、改 UI、调整行为时，不要只给方案。先读代码，再修改，再验证，最后汇报结果。
- 用户说“严格参考”时，按硬性要求处理。必须找到被参考的页面或组件，尽量复用它的结构、类名、间距、视觉状态和交互方式。
- 用户纠正“字段”“列”“按钮”等产品词时，按字面产品含义执行。表格里的“加一个字段”通常就是新增独立列，不是把文字塞进已有单元格。

## 本地运行约定

- 后端开发服务使用端口 `3010`。
- 前端开发服务使用端口 `5011`。
- 做浏览器验证前，先确认目标端口正在监听。
- 页面跳到登录页时，用演示账号登录：
  - Labeler 页面：账号 `labeler`，密码任意非空。
  - AI Agent 页面：账号 `agent`，密码任意非空。
  - Owner 页面：账号 `owner`，密码任意非空。

## 实现原则

- 先读现有实现，再动手。优先沿用项目里的模式，不要自己另起一套。
- 改动要小而准。不要顺手重构无关文件，不要清理不属于本次任务的脏工作区。
- 保留用户已有改动。除非用户明确要求，禁止 reset、checkout 或回滚文件。
- 搜索优先用 `rg`，手动编辑文件用 `apply_patch`。
- 复用已有 API helper、DTO、组件和样式约定，避免复制请求逻辑或重复造轮子。
- UI 文案要克制。不要主动加解释型大段说明，除非用户明确需要。

## 任务 ID 规则

- 页面展示的任务 ID 必须和任务管理页一致。
- 需要 `T-0001` 这类用户可见任务编号时，使用 `apps/web/src/pages/owner/taskDisplayId.ts` 里的 `createTaskDisplayIdMap`。
- 不要在用户界面里把后端原始 ID（例如 `task_qa`）当作业务任务 ID 展示。
- 如果某个页面的 DTO 里只有 `taskId`，但要展示业务编号，就通过 `listTasks()` 拉 `/tasks`，再用同一套映射生成展示 ID。
- 表格里需要任务 ID 字段时，新增独立的 `任务ID` 列，结构参考任务管理表：`td.task-table__id` 里面放 `code`。

## 表格和布局

- 改表格时，必须同时检查表头顺序、单元格顺序、空状态 `colSpan`、搜索行为和行点击行为。
- 用户要求参考任务管理表时，先复用任务管理表已有的 class 和列/单元格结构，再考虑新增页面专用 CSS。
- 用户要求独立字段时，不要把编号、状态等信息拼到任务名或其他已有单元格里。
- 表格内容默认要横向对齐在同一行，除非设计明确需要上下堆叠。
- 增删列时，要同步更新测试里的表头、单元格数量和空状态断言。

## 标注员工作台和标注台

- 标注台顶部左侧显示当前任务名和用户可见任务 ID。
- 标注台关闭按钮放在顶部最右侧操作区。
- 从标注员工作台列表进入标注台时，要把展示用任务 ID 放进路由 state，保证标注台显示和工作台一致的 `T-0001`。
- 同一个任务内切题时，要继续保留这个展示任务 ID。

## 导出和字段顺序

- 导出文件里的上传源字段顺序必须保留上传文件原始顺序。
- 标注字段顺序按 Labeler 画布从上到下的顺序。
- 有明确 schema 顺序或上传顺序时，不要依赖对象 key 的自然遍历顺序。

## AI 机审队列

- AI 机审队列表格按任务级批次展示，不按单题拆开。
- 最左侧任务 ID 列必须和任务管理页一致。
- “AI 建议”里的“建议通过”气泡左侧要有任务管理状态同款绿色圆点。
- “建议通过”“建议打回”“等待预审”“失败”等状态要保持清楚区分，不要引入无关的新配色体系。

## 测试和验证

- 行为变化尽量先补测试，再写实现。
- 前端改动先跑相关窄测试，再跑 `pnpm --filter @labelhub/web typecheck`。
- 后端改动跑触达模块的 API 测试和类型检查。
- 重要 UI 改动后，尽量用浏览器或 Playwright 打开正在运行的页面验证。
- 浏览器验证不要只看截图，要检查真实 DOM 和关键 computed style。
- 没有新鲜验证结果前，不要声称已经修好。

## 汇报方式

- 最后汇报要说明改了什么、主要文件、验证命令。
- 没能验证的地方要明确说出来。
- 回答保持简洁、直接、可执行。
